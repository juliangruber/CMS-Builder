<%= form_for(@relationship) do |f| %>
  <% if @relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relationship.errors.count, "error") %> prohibited this relationship from being saved:</h2>

      <ul>
      <% @relationship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  1 <%= f.select(:from_id, ContentType.all.collect {|c| [c.name, c._id]}) %>
  has
  <%= f.select(:type, [[:no,0],[:one,1],[:many,2]]) %>
  <%= f.select(:to_id, ContentType.all.collect {|c| [c.name, c._id]}) %>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
