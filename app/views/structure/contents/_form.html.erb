<%= form_for(@structure_content) do |f| %>
  <% if @structure_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@structure_content.errors.count, "error") %> prohibited this structure_content from being saved:</h2>

      <ul>
      <% @structure_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
